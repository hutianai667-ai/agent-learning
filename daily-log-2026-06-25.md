# Daily Log - 2026-06-25

## 今天阶段

Stage 0 总复习与综合验收

## 今天阅读

今天复习了 Stage 0 的主要内容：

- Git 最小流程：`status`、`add`、`commit`、`push`
- 本地仓库和远程 GitHub 仓库的关系
- branch 和 Pull Request 的基本作用
- Python 文件读写：`r`、`w`、`a`
- JSON 的读取和写入：`json.load`、`json.dump`
- 公开 API 的调用：`urllib.request.urlopen`
- API key 安全：`.env`、`.env.example`、`.gitignore`

## 今天实操

今天完成了 Stage 0 综合验收脚本：

- 创建 `week-01/scripts/stage0_final_check.py`
- 读取本地 JSON 文件 `week-01/data/stage0-profile.json`
- 调用 GitHub 公开 API `https://api.github.com/users/torvalds`
- 整理本地数据和 API 数据
- 输出结果到 `week-01/outputs/stage0-final-check.json`
- 使用 Git 完成提交并 push 到 GitHub

## 今天理解

今天这个练习把 Stage 0 的内容串起来了：

1. 本地 JSON 文件可以通过 Python 读取。
2. 公开 API 返回的数据也是 JSON，可以用 Python 处理。
3. Python 可以把处理后的结果重新写入本地输出文件。
4. Git 用来记录本地修改。
5. GitHub 用来保存远程仓库。
6. `.env` 和 API key 不能上传，安全规则必须提前设置。

目前来看，今天的综合练习内容基本都能理解。

## 今天遇到的问题

第一次 `git push` 时遇到了 SSL/TLS 连接失败，说明本地已经 commit，但远程还没有上传成功。

通过再次执行 `git push` 后，成功同步到 GitHub。

## 今天 Git 状态

最终 `git status` 显示：

```text
Your branch is up to date with 'origin/main'.
nothing to commit, working tree clean