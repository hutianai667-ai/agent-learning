# Daily Log - 2026-07-09

## 今天阶段

- 当前阶段：A1
- 今天任务：让 Codex 检查 Stage 2 prompt 迭代练习文件
- 任务风险：低风险，只读检查任务

## Agent 使用记录

- 我给 agent 的 prompt：检查 `stage-02\prompt-iteration-practice.md`，找出最多 5 个具体问题，并说明问题是什么、为什么是问题、怎么改、是否必须改
- agent 是否先说明计划：是
- agent 是否等我确认后执行：是
- agent 准备读取哪些文件：`stage-02\prompt-iteration-practice.md`
- agent 实际读取哪些文件：`stage-02\prompt-iteration-practice.md`
- agent 是否修改文件：否
- 输出哪里可用：明确说明了文件用途，指出了 Markdown 结构、测试输入、实际输出、few-shot 示例、最终 prompt 等具体问题，并区分是否影响 Stage 2 验收
- 输出哪里不可用：部分建议没有给出可直接参考的改写示例，比如建议补 few-shot 示例，但没有把示例内容完整写出来
- 我做了什么人工检查：确认任务只读、没有修改文件，并判断最值得优先改的是“每一版缺少实际输出”，因为这会影响日后复盘

## 安全边界

- 是否涉及 `.env`：否
- 是否涉及 API key：否
- 是否涉及密码或隐私文件：否
- 是否读取、输出、修改敏感文件：否

## 今日理解

- 今天学到的重点：检查任务不只是找问题，还要看建议是否具体到可以执行
- 今天纠正的误解：agent 给出“怎么改”还不够，最好同时给出示例，方便人工判断是否采用
- 今天发现 agent 的问题：有些建议方向正确，但缺少完整示例，实操性还可以提高

## 明日计划

- 明天只做一件最重要的事：继续 A1，让 Codex 拆解下一周任务，观察它是否能给出可执行的小步骤
- 如果卡住 20 分钟，我要：要求 Codex 把建议改成“问题 + 原因 + 示例 + 是否必须做”的格式

## 今日额外任务：A1 第 5 个任务预演

### 任务名称

让 Codex 拆解下一周 A2 上半任务。

### 任务风险

低风险，只读和规划任务。

### Agent 使用记录

- agent 是否先说明计划：是
- agent 是否读取文件：是，只读取本地学习计划中的 A2 上半内容
- agent 是否修改文件：否
- agent 是否跳到 A2 执行：否，没有创建 `AGENTS.md`，没有测试 agent 行为，也没有连接 MCP
- 输出哪里可用：按 5 天拆解了 A2 上半学习任务，并明确每一天的产出
- 输出哪里不可用：计划还缺少每一步的具体做法和遇到问题时的处理办法
- 我的人工检查：确认这是 A1 低风险规划任务，不是提前执行 A2

### 今日理解补充

A1 的“拆解下一周任务”不是提前进入下一阶段，而是训练我判断 agent 能不能把未来任务拆成小步骤。

好的拆解不仅要说明每天做什么，还要说明：

- 产出是什么
- 为什么这样做
- 遇到问题怎么调整
- 哪些操作需要人工确认

## A1 验收记录 - 2026-07-09

### A1 已完成任务

1. 总结仓库结构和已有笔记。
2. 解释 Stage 2 prompt 迭代文件。
3. 生成 daily log 模板草稿。
4. 检查 Stage 2 prompt 练习文件。
5. 拆解下一周 A2 上半任务。

### A1 验收结果

- [x] 跑过至少 5 个非 hello-world 任务。
- [x] 能说出 CLI agent 和网页聊天的区别。
- [x] 知道 CLI agent 能操作本机文件，因此需要安全边界。

结论：A1 通过。

### 今日理解

CLI agent 和网页聊天最大的区别是：CLI agent 可以读取、修改、生成本机文件，也能执行命令；网页聊天主要是回答问题和生成文本。

因此 CLI agent 必须设置安全边界，否则可能因为任务描述不清楚而误读、误改文件，甚至造成不可回退的错误。

### 需要人工确认的操作

- 修改文件。
- 删除文件。
- 读取或处理敏感文件。
- git push / 上传 GitHub。
- 不明确任务范围时继续执行。

### 我以后给 Codex 的固定要求

- 先说明计划和准备读取/修改的文件。
- 不读取、不输出、不修改 `.env`、API key、密码或隐私文件。
- 每条建议都要包含：问题是什么、为什么是问题、怎么改、改完有什么效果。

### 进入 A2 前的准备

我希望未来 `AGENTS.md` 约束 agent：

- 只在指定目录内读取或输出文件。
- 不碰 `.env`、API key、账号密码等敏感内容。
- 删除文件前必须人工确认。
- 任务不明确时先询问，或先输出计划让我确认。
- 输出后自检是否合理，发现问题先修正再交付。