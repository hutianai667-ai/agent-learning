# Daily Log - 2026-06-26

## 今天阶段

Stage 1 第 2 节：Token / Context / Cost 估算意识

## 今天阅读

今天学习了输入 token、输出 token、context window 和 cost 的关系。

输入 token 包括用户输入、聊天历史、文件内容、工具返回结果等。
输出 token 主要是模型生成的回答、解释、代码、总结和列表。

## 今天实操

今天完成了 token / cost 风险判断练习，并把坏 prompt 改写成更适合 CLI Agent 的版本。

## 今天理解

Token 和 cost 不只取决于问题本身，也取决于读取范围、输出长度、聊天历史、工具返回内容和任务轮次。

更好的 prompt 应该明确读取范围、安全边界、任务目标、输出格式和输出长度。

## 今天纠正

`bullet` 是项目符号列表，通常是 `- 第一条` 这种格式，不一定是 `1、2、3`。

## 安全边界

今天没有读取、输出、修改 `.env`、API key、密码文件。

## 明天计划

继续 Stage 1，练习 temperature 对比。